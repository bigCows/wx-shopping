import { AxiosError, AxiosRequestConfig, AxiosResponse } from "../config/HttpConfig";
export default function createError(message: string, config: AxiosRequestConfig, code?: string, request?: any, response?: AxiosResponse): AxiosError<any>;
export declare class ErrorMessage extends Error implements AxiosError {
    code: string;
    config: AxiosRequestConfig;
    isAxiosError: boolean;
    request: any;
    response: AxiosResponse<any>;
    stack: string;
    toJSON: Function;
    message: string;
    constructor(message: string);
}
